import 'package:flutter/material.dart';

class AppColors {
  // App base colors
  static const Color primary = Color(0xFF00695C); // Deep emerald green
  static const Color primaryVariant = Color(0xFF004D40);
  static const Color secondary = Color(0xFF80CBC4); // Soft teal
  static const Color secondaryVariant = Color(0xFF26A69A);
  static const Color background = Color(0xFFF3FBF6); // Very light mint/green
  static const Color surface = Color(0xFFFFFFFF);    // White for cards
  static const Color error = Color(0xFFD32F2F);
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onSecondary = Color(0xFF00332F);
  static const Color onBackground = Color(0xFF1B2B24);
  static const Color onSurface = Color(0xFF1B2B24);
  static const Color onError = Color(0xFFFFFFFF);
  static const Color lightGrey = Color(0xFFE0F2F1); // Soft greenish grey
  static const Color darkGrey = Color(0xFF455A64);  // Blue-grey
}

// Market Owner Palette - Rich, professional blues
class MarketOwnerColors {
  static const Color primary = Color(0xFF1565C0);      // Deep blue
  static const Color secondary = Color(0xFF64B5F6);    // Light blue
  static const Color accent = Color(0xFF0D47A1);       // Dark blue
  static const Color background = Color(0xFFE3F2FD);   // Very light blue
  static const Color surface = Color(0xFFFFFFFF);      // White
  static const Color text = Color(0xFF0D3C61);         // Dark blue text
  static const Color textLight = Color(0xFF5C7D99);    // Light blue text
  static const Color icon = Color(0xFF1976D2);         // Medium blue for icons

  // Adding the missing "on" colors
  static const Color onPrimary = Color(0xFFFFFFFF);    // White text on primary
  static const Color onSecondary = Color(0xFF0D3C61);  // Dark blue text on secondary
  static const Color onAccent = Color(0xFFFFFFFF);     // White text on accent
  static const Color onBackground = Color(0xFF0D3C61); // Dark blue text on background
  static const Color onSurface = Color(0xFF0D3C61);    // Dark blue text on surface
  static const Color onError = Color(0xFFFFFFFF);      // White text on error
}

// Farmer Palette - Earthy, natural greens and browns
class FarmerColors {
  static const Color primary = Color(0xFF33691E);      // Deep green
  static const Color secondary = Color(0xFF8BC34A);    // Light green
  static const Color accent = Color(0xFF795548);       // Brown
  static const Color background = Color(0xFFF1F8E9);   // Very light green
  static const Color surface = Color(0xFFFFFFFF);      // White
  static const Color text = Color(0xFF33691E);         // Dark green text
  static const Color textLight = Color(0xFF7CB342);    // Light green text
  static const Color icon = Color(0xFF558B2F);         // Medium green for icons

  // Adding the missing "on" colors
  static const Color onPrimary = Color(0xFFFFFFFF);    // White text on primary
  static const Color onSecondary = Color(0xFF33691E);  // Dark green text on secondary
  static const Color onAccent = Color(0xFFFFFFFF);     // White text on accent
  static const Color onBackground = Color(0xFF33691E); // Dark green text on background
  static const Color onSurface = Color(0xFF33691E);    // Dark green text on surface
  static const Color onError = Color(0xFFFFFFFF);      // White text on error
}

// Client Palette - Friendly, warm colors
class ClientColors {
  static const Color primary = Color(0xFFFF7043);      // Warm orange
  static const Color secondary = Color(0xFFFFAB91);    // Light coral
  static const Color accent = Color(0xFFE64A19);       // Deep orange
  static const Color background = Color(0xFFFFF3E0);   // Very light orange (fixed the hex code)
  static const Color surface = Color(0xFFFFFFFF);      // White
  static const Color text = Color(0xFF663D2C);         // Dark warm brown text
  static const Color textLight = Color(0xFFBF7C63);    // Light brown text
  static const Color icon = Color(0xFFF4511E);         // Medium orange for icons

  // Adding the missing "on" colors
  static const Color onPrimary = Color(0xFFFFFFFF);    // White text on primary
  static const Color onSecondary = Color(0xFF663D2C);  // Dark brown text on secondary
  static const Color onAccent = Color(0xFFFFFFFF);     // White text on accent
  static const Color onBackground = Color(0xFF663D2C); // Dark brown text on background
  static const Color onSurface = Color(0xFF663D2C);    // Dark brown text on surface
  static const Color onError = Color(0xFFFFFFFF);      // White text on error
}